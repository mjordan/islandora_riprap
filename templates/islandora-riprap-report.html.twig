<div {{ attributes }}>
    <div class="islandora_riprap_report">
    {% for media_name, urls in report %}
      <h4>{{ media_name }}</h4>
        <ul>
        {% for media_url in urls %}
          <li>{{ media_url }}</li>
        {% endfor %}
        </ul>
    {% endfor %}
</div>
